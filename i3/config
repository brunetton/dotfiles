# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
# bindsym $mod+Shift+t exec xfce4-terminal

# kill focused window
bindsym $mod+Shift+w kill

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
#bindsym $mod+Shift+j move left
#bindsym $mod+Shift+k move down
#bindsym $mod+Shift+l move up
#bindsym $mod+Shift+m move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
bindsym $mod+z layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+q focus parent

# focus the child container
#bindsym $mod+d focus child

# # switch to workspace
# bindsym $mod+ampersand workspace 1
# bindsym $mod+eacute workspace 2
# bindsym $mod+quotedbl workspace 3
# bindsym $mod+apostrophe workspace 4
# bindsym $mod+parenleft workspace 5
# bindsym $mod+minus workspace 6
# bindsym $mod+egrave workspace 7
# bindsym $mod+underscore workspace 8
# bindsym $mod+ccedilla workspace 9
# bindsym $mod+agrave workspace 10
#
# # move focused container to workspace
# bindsym $mod+Shift+ampersand move container to workspace 1
# bindsym $mod+Shift+eacute move container to workspace 2
# bindsym $mod+Shift+quotedbl move container to workspace 3
# bindsym $mod+Shift+apostrophe move container to workspace 4
# bindsym $mod+Shift+5 move container to workspace 5
# bindsym $mod+Shift+minus move container to workspace 6
# bindsym $mod+Shift+egrave move container to workspace 7
# bindsym $mod+Shift+underscore move container to workspace 8
# bindsym $mod+Shift+ccedilla move container to workspace 9
# bindsym $mod+Shift+agrave move container to workspace 10

# reload the configuration file
#bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        #bindsym j resize shrink width 10 px or 10 ppt
        #bindsym k resize grow height 10 px or 10 ppt
        #bindsym l resize shrink height 10 px or 10 ppt
        #bindsym m resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        # status_command i3status
        status_command i3blocks -c /home/bruno/dev/dotfiles/i3-blocks/i3blocks.conf
        tray_output none  # I don't want apps icons to be in i3 bar, but in xfce bar
}



# PERSO BDUYE

# Hide windows borders
#hide_edge_borders both
# I love tabs
workspace_layout tabbed

# bindsym $mod+Ctrl+Right workspace next
# bindsym $mod+Ctrl+Left workspace prev
# bindsym $mod+Ctrl+Shift+Left move container to workspace prev; workspace prev
# bindsym $mod+Ctrl+Shift+Right move container to workspace next; workspace next


## i3-workspacesgrid config
# Direct acces to desktops
bindsym $mod+ampersand exec curl http://localhost:7088/jump/1
bindsym $mod+eacute exec curl http://localhost:7088/jump/2
bindsym $mod+quotedbl exec curl http://localhost:7088/jump/3
bindsym $mod+apostrophe exec curl http://localhost:7088/jump/4
bindsym $mod+parenleft exec curl http://localhost:7088/jump/5
bindsym $mod+minus exec curl http://localhost:7088/jump/6
bindsym $mod+egrave exec curl http://localhost:7088/jump/7
bindsym $mod+underscore exec curl http://localhost:7088/jump/8
bindsym $mod+ccedilla exec curl http://localhost:7088/jump/9
bindsym $mod+agrave exec curl http://localhost:7088/jump/10
# Direct sending containers to workspaces
bindsym $mod+Shift+ampersand exec curl http://localhost:7088/send/1
bindsym $mod+Shift+eacute exec curl http://localhost:7088/send/2
bindsym $mod+Shift+quotedbl exec curl http://localhost:7088/send/3
bindsym $mod+Shift+apostrophe exec curl http://localhost:7088/send/4
bindsym $mod+Shift+5 exec curl http://localhost:7088/send/5
bindsym $mod+Shift+minus exec curl http://localhost:7088/send/6
bindsym $mod+Shift+egrave exec curl http://localhost:7088/send/7
bindsym $mod+Shift+underscore exec curl http://localhost:7088/send/8
bindsym $mod+Shift+ccedilla exec curl http://localhost:7088/send/9
bindsym $mod+Shift+agrave exec curl http://localhost:7088/send/10
# Moving througth workspaces
bindsym $mod+Ctrl+Up exec curl http://localhost:7088/up
bindsym $mod+Ctrl+Down exec curl http://localhost:7088/down
bindsym $mod+Ctrl+Left exec curl http://localhost:7088/left
bindsym $mod+Ctrl+Right exec curl http://localhost:7088/right
# Sending containers to workspaces
bindsym $mod+Ctrl+Shift+Up exec curl http://localhost:7088/send/up
bindsym $mod+Ctrl+Shift+Down exec curl http://localhost:7088/send/down
bindsym $mod+Ctrl+Shift+Left exec curl http://localhost:7088/send/left
bindsym $mod+Ctrl+Shift+Right exec curl http://localhost:7088/send/right


exec --no-startup-id "~/dev/i3-workspacesgrid/i3-grid.py start-server"


for_window [title="MediaInfo"] floating enable
#for_window [class="Gimp"] floating enable
#for_window [class="VirtualBox"] floating disable
#for_window [class="VirtualBox" title="Windows.*"] floating enable
#for_window [class="VirtualBox" title=".*Virtual Machine.*"] floating enable
# for_window [class="VirtualBox"] floating enable
for_window [class="(j|J)ack-keyboard"] floating enable
for_window [title="^setBfree$"] floating enable
for_window [title="^GMIDImonitor$"] floating enable
for_window [class="(j|J)alv"] floating enable
for_window [class="(x|X)sane"] floating enable
for_window [class="(p|P)eazip"] floating enable
for_window [class="(q|Q)jack(c|C)tl.*"] floating enable
for_window [class="(g|G)nome-calculator"] floating enable
for_window [class="(p|P)avucontrol"] floating enable
for_window [title="Magnus"] floating enable
for_window [title="feh"] floating enable
for_window [title="CustomScript"] floating enable

# Floating disabled
for_window [class="Xfdesktop" title="Bureau"] floating disable
for_window [class="REAPER" title="^MIDI.*"] floating disable  # Editeur MIDI Reaper
for_window [class="neverputt"] floating disable

# Organize workspaces
set $souk 10
assign [class="Zim"] → $souk
assign [class="Pidgin"] → $souk


exec --no-startup-id parcellite
exec --no-startup-id zim
exec --no-startup-id setxkbmap -option caps:none
exec --no-startup-id nm-applet
# exec --no-startup-id redshift-gtk
exec --no-startup-id pnmixer
exec --no-startup-id sxhkd
exec --no-startup-id blueman-applet
exec --no-startup-id ~/bin/xbanish
exec --no-startup-id ulauncher --hide-window
exec --no-startup-id xfce4-session

# exec --no-startup-id xfsettingsd --replace
# exec --no-startup-id xrandr --dpi 80
# reload

# exec --no-startup-id /usr/lib/gnome-settings-daemon/gsd-xsettings
# exec_always --no-startup-id gnome-power-manager

bindsym $mod+Shift+c exec google-chrome
bindsym $mod+Shift+f exec firefox --profile ~/.mozilla/firefox/1c3xke53.default-release-2
bindsym $mod+Ctrl+Shift+f exec firefox --ProfileManager
# bindsym $mod+Shift+z exec zim --standalone
# bindsym $mod+Shift+y exec pidgin
bindsym $mod+Shift+m exec clementine
# bindsym $mod+Shift+e exec nemo --no-desktop
bindsym $mod+Shift+e exec dolphin
# bindsym $mod+Shift+h exec ~/bin/hibernate_confirm
# bindsym $mod+Shift+s exec ~/bin/suspend_confirm
bindsym $mod+Shift+t exec xfce4-terminal
bindsym $mod+Shift+h exec hamster-snap
bindsym $mod+d exec --no-startup-id ulauncher-toggle
bindsym $mod+s exec flameshot gui
bindsym $mod+Shift+s exec "sleep 3; flameshot gui"
bindsym --release $mod+Shift+x exec --no-startup-id xkill

# Click
# bindings $mod+c exec --no-startup-id xdotool click 1

# Screenshot
bindsym Print exec gnome-screenshot --interactive
# volume control
bindsym XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ '-5%' && pkill -RTMIN+10 i3blocks
bindsym XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ '+5%' && pkill -RTMIN+10 i3blocks
bindsym XF86AudioMute exec pactl set-sink-mute @DEFAULT_SINK@ toggle && pkill -RTMIN+10 i3blocks
# brightness control
# bindsym XF86MonBrightnessDown exec xbacklight -steps 1 -time 0 -dec 5
# bindsym XF86MonBrightnessUp exec xbacklight -steps 1 -time 0 -inc 10
# Switch to xrandr since xbacklight produces an error and does not produce any effect since some weeks.

#bindsym XF86MonBrightnessDown exec ~/.config/i3blocks/brightness-via-xrandr -0.1 && pkill -RTMIN+20 i3blocks
#bindsym XF86MonBrightnessUp exec ~/.config/i3blocks/brightness-via-xrandr +0.1 && pkill -RTMIN+20 i3blocks
